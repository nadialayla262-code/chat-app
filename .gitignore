# PocketBase runtime data (the database lives here — back it up, never commit it)
pb_data/
# Downloaded PocketBase binary
bin/
# OS / editor noise
.DS_Store
*.log
node_modules/
# Homei worker secrets and append-only log
workers/.homei-password
workers/log/
